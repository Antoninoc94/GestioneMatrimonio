# Copia questo file in .env e imposta i valori reali
# cp .env.example .env

# OBBLIGATORIO: stringa segreta per JWT (genera con: openssl rand -hex 32)
JWT_SECRET=cambia_questa_stringa_con_qualcosa_di_sicuro
